package com.saj.rickandmorty.repositories

import com.saj.rickandmorty.models.ShowCharacter

open class ShowCharactersRepository {
    fun fetchShowCharacters(): List<ShowCharacter> {
        return emptyList()
    }
}