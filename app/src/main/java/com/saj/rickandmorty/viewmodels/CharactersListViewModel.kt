package com.saj.rickandmorty.viewmodels

import com.saj.rickandmorty.models.ShowCharacter

class CharactersListViewModel {
    fun fetchCharacters(): List<ShowCharacter> {
        return emptyList()
    }
}